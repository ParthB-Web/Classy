<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<title>PARTH B.</title>
<link href="https://fonts.googleapis.com/css2?family=EB+Garamond:wght@300;400&family=Montserrat:wght@300;400;500&display=swap" rel="stylesheet" />
<style>
  :root {
    --bg: #0a0a0a;
    --fg: #e8e8e8;
    --muted: #8e8e8e;
    --accent: #d2b48c;
    --accent-soft: rgba(210,180,140,0.12);
    --edge: rgba(255,255,255,0.08);
    --card: rgba(255,255,255,0.02);
    --font-serif: "EB Garamond", serif;
    --font-sans: "Montserrat", sans-serif;
  }
  body {
    margin: 0;
    background: var(--bg);
    color: var(--fg);
    font-family: var(--font-serif);
    -webkit-font-smoothing: antialiased;
  }
  /* page fade-in */
  body { opacity: 0; animation: fadeIn 1.2s ease forwards; }
  @keyframes fadeIn { from { opacity: 0; } to { opacity: 1; } }

  /* name reveal */
  .name {
    font-size: 4.8rem;
    font-weight: 300;
    letter-spacing: 3px;
    text-align: center;
    margin-top: 6rem;
    margin-bottom: 1.2rem;
    opacity: 0;
    animation: nameReveal 1.8s cubic-bezier(.25,.8,.25,1) forwards;
  }
  @keyframes nameReveal {
    0% { opacity: 0; filter: blur(12px); transform: translateY(40px); }
    60% { opacity: 1; filter: blur(0px); transform: translateY(0px); }
    100% { opacity: 1; }
  }

  .subtitle {
    text-align: center;
    margin-bottom: 5rem;
    font-family: var(--font-sans);
    font-size: 1rem;
    color: var(--muted);
    opacity: 0;
    animation: fadeUp 1.4s .9s ease forwards;
  }
  @keyframes fadeUp {
    from { opacity: 0; transform: translateY(20px); }
    to { opacity: 1; transform: translateY(0); }
  }

  .frame {
    max-width: 960px;
    margin: 0 auto;
    padding: 0 1.5rem 7rem;
  }

  .section { margin-bottom: 4rem; }

  .card {
    background: var(--card);
    border: 1px solid var(--edge);
    padding: 2.4rem;
    border-radius: 10px;
    backdrop-filter: blur(12px);
    transition: 0.45s ease;
    position: relative;
    overflow: hidden;
  }

  /* gold mist */
  .card::before {
    content: "";
    position: absolute;
    top: 0; left: 0;
    width: 100%; height: 100%;
    background: radial-gradient(circle at top left, var(--accent-soft), transparent 60%);
    opacity: 0;
    transition: 0.5s ease;
  }
  .card:hover::before { opacity: 1; }

  .card:hover {
    border-color: var(--accent);
    transform: translateY(-6px);
    box-shadow: 0 18px 38px rgba(0,0,0,0.35);
  }

  .card h2 {
    font-size: 2rem;
    color: var(--accent);
    margin-bottom: 1rem;
  }

  .card p {
    color: var(--muted);
    line-height: 1.75;
    font-size: 1.05rem;
  }

  a {
    color: var(--accent);
    font-family: var(--font-sans);
    text-decoration: none;
    border-bottom: 1px solid transparent;
    transition: 0.3s ease;
  }
  a:hover { border-bottom: 1px solid var(--accent); }

  .footer {
    text-align: center;
    margin-top: 8rem;
    font-family: var(--font-sans);
    color: var(--muted);
    font-size: 0.85rem;
    opacity: 0.7;
  }
</style>
</head>
<body>
  <div class="name">PARTH B.</div>
  <div class="subtitle">Work rooted in perception, precision, and moral clarity.</div>

  <div class="frame">

    <div class="section">
      <div class="card">
        <h2>Watcher: The Cost of Coherence</h2>
        <p>A document shaped by accuracy rather than performance. A quiet examination of what truth demands.</p>
        <p><a href="#">Read the Book</a></p>
      </div>
    </div>

    <div class="section">
      <div class="card">
        <h2>Substack</h2>
        <p>All writing lives here — ongoing work on coherence, perception, and internal mechanics.</p>
        <p><a href="#">Visit Substack</a></p>
      </div>
    </div>

    <div class="section">
      <div class="card">
        <h2>Goodreads</h2>
        <p>A quieter archive — cataloged reading, influence trails, and the surrounding ecosystem of thought.</p>
        <p><a href="#">Goodreads Profile</a></p>
      </div>
    </div>

    <div class="section">
      <div class="card">
        <h2>About</h2>
        <p>Work concerned with the discipline of seeing — the structural layer beneath behavior, belief, and motive.</p>
        <p><a href="#">More</a></p>
      </div>
    </div>

    <div class="footer">© PARTH B.</div>
  </div>
</body>
</html>
